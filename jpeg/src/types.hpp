#pragma once

#include <vector>

namespace jpeg {

// generic types for better data interpretation
using bit = bool;
using byte = unsigned char;
using bytes = std::vector<byte>;

}  // namespace jpeg
